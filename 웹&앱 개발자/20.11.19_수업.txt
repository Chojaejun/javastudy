1. 전송되는 데이터의 크기
- 바이트스트림: 1바이트씩 전송, All(다 사용할 수 있음), 멀티미디어파일, ~Stream
- 문자스트림: 2바이트씩 전송, text, ~er

========================

바이트스트림:
입력과 관련된 최상위 부모클래스: InputStream
출력과 관련된 최상위 부모클래스: OutputStream

==============
1. Stream 변수 선언
2. try ~ catch ~ finally 구문을 만듦.
3. finally 구문에 try ~ catch
4. finally의 try에서 close( )호출, catch 구현
5. 원 catch 구현
6. try 구현: 스트림 객체 만듦


ora-01033 oracle initialization or shutdouwn in progress < 이 에러 메세지가 뜬 사람

sqlplus "/as sysdba"

spl> recover database;
sql> alter database open;
sql> shutdown immediate;
sql> startup
 